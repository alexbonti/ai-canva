rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Board images — any authenticated user can read/write
    match /boards/{boardId}/images/{imageName} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
